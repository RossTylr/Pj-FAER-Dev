# BUILD_S3 v2 — Step 3: doctrine-faithful movement (M2+M3) + multi-POI
### THREE sessions against one file: R (reconciliation, docs-only, ungated) → α (gate word 1) → β (gate word 2). Authority: docs/MAAFI/MAAFI_VERDICT.md ▸ ROUTING_SEMANTICS_NOTE v3 (signed) ▸ this file. Evidence: S3_PREBUILD (Q1–Q10) + S3_FOLLOWUP (Q11–Q19) + the pre-flight review (its F1–F10 rulings are folded in below).
### RULE-3 DECLARATION: heaviest intrinsic step of the programme — ≈180–260 intrinsic LOC total (α ≈90–140, β ≈90–120), **RAW convention: max(added,removed) incl. comments, dual-mode duplication counted** (S2-D precedent). Gate is PER-SESSION: one human word covers α's slices, a second covers β's. Precedence clause applies.
### ZERO-REGEN POSTURE: coin's golden expected inert under every slice (legacy walk untouched · N=1 keying preserved · recompute idempotent — triage_decisions is pure · waypoints config-gated off · transport changes toggled off · golden-hour conditioning coin-inert, FACILITY_ARRIVAL 16 == TREATMENT_START 16). ANY O1 diff = STOP and report. Regeneration is NOT sanctioned anywhere in S3.

## SESSION R — reconciliation (pre-α preconditions; docs-only, no src/tests; no gate word required)
R1. Commit ROUTING_SEMANTICS_NOTE v3 (human supplies) → docs/MVP/ROUTING_SEMANTICS_NOTE.md. Signed block (initials/date) present = its authority check. **Verbatim-lift of the cited doctrine paragraphs from source PDFs is DEFERRED** (cannot be lifted in-session) — register row records that refs are authoritative pointers pending enrichment.
R2. Commit THIS file → docs/MVP/BUILD_S3.md (process rule 1).
R3. Execute the register reconciliation → CURRENT.md **Rev 4** (round-B closures/updates/new rows + traceability appendix mapping).
R4. Minute in the ledger: S1's AC amendments were applied INLINE without labels; labelled blocks begin at S3-AMEND-1 (this file).
R5. Verify authority path docs/MAAFI/MAAFI_VERDICT.md; correct any remaining stale pointers found (stale-path class).
R-close: one or two docs commits · suite untouched (163) · checker ALL PASS · report SHAs. **α may not fire until R is closed.**

**Baseline for α:** HEAD = R-close SHA (record at α step 0) · 163 green · comparison lane OPEN.

## Design of record (signed note + rulings; review findings folded)
- **M2 waypoints:** routing yields a clinical DESTINATION; graph-mode intermediates with `waypoint_allowed: true` (per-facility config, **default False** = today's behaviour) are waypoints — measured beds=0 signature (TRANSIT_END + FACILITY_ARRIVAL + TRANSIT_START, no TREATMENT_*), `waypoint=true` passed to create_event ONLY when true (kwargs→metadata; golden-safe). Continuity floor §0101.3 recorded; config-level enforcement; platform care-levels = register row. ⟨HUMAN VETO stands⟩ Walk modes have no intermediates → unaffected.
  - **Axis note (recorded, not resolved):** the signed note rules the floor **platform-capability-relative**; the config proxy enforces it **per-facility**. The gap between the ruled axis and the built axis IS the substance of the standing veto and of the platform-care-levels register row. Named here so the divergence is on record rather than silent.
- **M3 boundary recompute:** decisions dict (requires_dcs + bypass_role1) recomputed at loop-top + hold-retry. Idempotent (pure over triage). The defect is sharper than staleness: engine.py:884-886 already updates the requires_dcs FIELD but not bypass_role1 and not the loop-local DICT — field and dict can disagree mid-journey today. Recompute heals both. Deterioration confers bypass; withdrawal-on-improvement is mechanically supported but currently unreachable (promote-only, CP4 gate #7) — minuted, register note, no behavioural demotion test (F6).
- **Hold truth:** `PatientState.HOLDING` ALREADY EXISTS (enums, "Treated but waiting for downstream capacity") — slice 1 SETS it in the hold loop (F-verified, ~0 LOC) + persists `intended_destination` on the entity. Never a disposition → canonical log unaffected.
  - **RULE (G8): `intended_destination` is engine-internal.** It appears in NO event payload and NO roster row. The roster side is already policed by the `roster_row` whitelist (data/roster.py) + I-7 clause 3; the event side gains an explicit slice-1 assertion (below).
- **Golden-hour integrity (D-A consequence, in-scope):** definition RULED (F3): stamp = **first R2 arrival AT WHICH treatment occurs** — waypointed-R2A-then-treated-R2B records R2B's arrival time. Untreated-before-cutoff R2 arrivals carry no stamp → total_tracked may shift on non-coin fixtures — **pre-declared reportable (5a)**. Coin inert (16==16). mining.py:259 gains the same condition; FacilityLoadView skips waypoint-flagged arrivals.
- **Multi-POI:** N ArrivalProcess instances, callback closed per-POI. Keying = function of POI count: N=1 keeps legacy forms (bare "arrivals", CAS-NNNN — byte-preservation proven by test); N≥2 scoped streams + id_prefix factories + POI-prefixed mascal_id. Per-POI MASCAL generators; weights = shares of theatre rate via `arrivals.per_poi`, Σ=1.0±ε guard. Detector untouched (#30).
  - **Cross-arm guard (F5/G3 ruling):** `scenario_stamp` (config/builder.py:116) gains a **`poi_count`** field; the pinned stamp determinism/sensitivity test (tests/test_slice1_config.py:93-98) is extended — POI-count sensitivity now EXPECTED. `require_comparable_arms(snapshot_a, snapshot_b)` lands in **config/guards.py**, reading `poi_count` off both stamps and raising `ValueError` on mismatch. SURFACE LOC. The AC-1.4 pairing test invokes the guard in its arrange step.
  - **`poi_id` override ruling:** `engine.run(poi_id=...)` **RAISES `ValueError`** when the scenario declares N≥2 POIs ("poi_id override is single-POI only"); N=1 keeps current semantics. `EnsembleBuilder.run(poi_id=)` (events/ensemble.py:246-250) forwards unchanged, inherits the raise, and gains a docstring deprecation note.
- **Transport (F4 split — TWO toggles, both default False):** `enable_origin_transport` = origin-dimension batcher + TRANSIT per (mode,origin) [`transit:<MODE>:<origin>`] — one coherent change (a batch spanning origins has no single origin to key on, so the batcher dimension is a *precondition* for the scoped stream, not a co-change); `enable_batched_turnaround` = turnaround on the batched path. OFF = byte-identical (asserted). Guards enforce origin_transport ON for analysis scenarios (GM-3 pattern). Default-flips → GM-4 bundle. **Re-measure pre-declared, two stages:** stage 1 origin_transport ON / turnaround OFF (isolates the provisional's subject); stage 2 both ON. Ratios per metric, both contrasts (routing pair; divert-exercising two-POI scenario) → S3_TRANSPORT_REMEASURE.md → provisional re-arbitrated per stage.

## 0. Scope fence
IN: R, α (slices 0–2), β (slices 3–5). OUT: #30 detector · platform care-levels · coordinate geometry · GoldenHourView/metrics unification (5a) · Step-4 · GM-4 · factory unification · POLYBIUS. engine.py:84-121 untouchable. MVP_ACCEPTANCE.md only via §AMEND. Shared-RNG mode never edited.

## SESSION α — hygiene · M3 · multi-POI
**Slice 0 (behaviour-zero):** delete dead `_arrival_process` (engine.py:1321-1339) · **POI-scan fix (F7 ruling): declared-POI-first selection, synthesised fallback only when zero declared, warning logged when both present** — behaviour-zero on every committed preset AND every inline-dict test fixture (verified: all five presets and all ten test fixtures declare their POI; zero synthesised POIs exist in the corpus, so builder.py:249-271 is unexercised and the both-present warning branch has no existing trigger); the synthesised-source feature is KEPT · interim `require_single_poi` guard (lifted slice 2) · harness close-list (F1): tests/harness.py:73-75 **+ test_capability_routing.py:98 + :273**. Suite green (163 + 0 new; step-0 baseline count asserted exactly). Golden identical. Commit.

**Slice 1 (M3 + hold truth), red-then-green:** recompute at loop-top + hold-retry · set HOLDING during holds · `intended_destination` field · hold-retry re-derives next_id (divert = decision change; casualty holds nothing — Q13.2).
Tests: **T-5-5b INVERTS to ==0** (red-as-written witnessed, then inverted green) · T-5-5a green · promotion-mid-hold divert test (T2→T1_SURGICAL at retry routes to surgical alternative) · **white-box recompute-symmetry unit test on triage_decisions (F6 — mechanism level, demotion branch)** · HOLDING assertion · **G8 assertion: the hold fixture's canonical log contains no `intended_destination` key** · **O4: STRUCTURAL GREEN — the PFC ceiling needs pfc_hours ≥ 24 h (engine.py:869, default max_hours=24.0), and O4's `_hold_timeout_override = 75.0` makes it unreachable; no promotion ⇒ no divert ⇒ O4 untouched. Record this argument, not a prediction** · golden identical. Suite green (163 + N; N enumerated in the commit body). Commit.

**Slice 2 (multi-POI), red-then-green:** N-instance wiring per design · scoped keys at N≥2 · per-POI factories/MASCAL · weights + Σ guard · `scenario_stamp.poi_count` + `require_comparable_arms` (config/guards.py) · `poi_id` N≥2 raise (engine + EnsembleBuilder docstring) · `require_single_poi` lifted.
Tests: AC-1.1 spawn proportions 0.7/0.3 ± 0.05 — fixture runs `mascal_enabled=False` (F8, minuted), **`@slow` marker, excluded from default suite, EXECUTED AND RECORDED at the α gate (F10)** · AC-1.3 conservation (drained, two-POI) · AC-1.4 amended form: determinism AND I-2-style pairing on a fixed two-POI scenario, **guard invoked in the arrange step** · **single-POI byte-preservation test** (coin digest unchanged — the asymmetry's proof) · extended stamp determinism/sensitivity test (POI-count sensitivity expected) · `require_comparable_arms` raises across the boundary · `poi_id` N≥2 raise test · O6 re-assert ×2 fixtures · mascal_id uniqueness. Golden identical.
Marker registration (G6): `[tool.pytest.ini_options]` gains `markers` + `addopts = -m "not slow"`. Commit body records that the Rule-7 `--regen-golden` invocation still collects O1 under the new addopts. Suite green (N passed, M deselected; both enumerated in the commit body). Commit.

**α gate — DoD:** □ reds witnessed (T-5-5b) □ O1 byte-identical throughout □ O4 structural argument recorded □ O6 ×2 □ conservation ×3 (≥1 two-POI) □ AC-1.1 slow-test executed+recorded (`uv run pytest -m slow`) □ default invocation excludes slow; deselected count M enumerated □ suite green; count = baseline + N new tests, N enumerated in the commit body □ I-1–I-7 green □ intrinsic LOC actual vs α estimate (RAW convention) □ deviations table vs this file □ STOP — β needs its own word.

## SESSION β — M2 · metric integrity · transport
**Slice 3 (waypoints + golden-hour), red-then-green:** `waypoint_allowed` parse (default False) · graph-mode destination-treats/intermediates-waypoint · continuity-floor code-comment (naming the axis note above) + register row · golden-hour per the RULED definition + mining fix + FacilityLoadView waypoint-aware.
Tests (F2 shape): **T-5-7 as-written STAYS GREEN — the defaults pin** · **NEW T-5-7w** (waypoint-enabled twin fixture) red-then-green asserting ns_treats empty · waypoint event-signature (exact Q14.2) · golden-hour exploit pinned (waypoint through R2 does NOT stamp) · **R2A-waypoint-then-R2B-treats records R2B (F3 case)** · treat-stop still stamps · **denominator shift (G7): the pre-named fixture IS T-5-7w — report `total_tracked` before/after conditioning on it** · test_facility_writer derivation re-checked · defaults regression: every-arrival-treats with no waypoint_allowed · golden identical. Suite green (count + deselected enumerated). Commit.

**Slice 4 (transport, two toggles), red-then-green:** build both flags per design.
Tests: **both-OFF: coin golden AND keyed digest byte-identical (load-bearing)** · origin_transport-ON plural fixture: batch-origin isolation + scoped-key census (I-3) · turnaround-ON: applied-assertion (downtime models agree) · guards enforce analysis-ON. Then the **two-stage pre-declared re-measure** → S3_TRANSPORT_REMEASURE.md, committed before any ratio is viewed; provisional re-arbitrated per stage at the β gate. Register row: default-flips → GM-4. Commit.

**Slice 5 (docs + AC):** apply **S3-AMEND-1/2/3** verbatim — the ONLY MVP_ACCEPTANCE.md edits · CURRENT.md Rev 5: rows CLOSED (bypass-staleness, hold-state) · rows NARROWED (**path-purity: resolved for waypoint-enabled scenarios; defaults unchanged by design — F2**) · rows added (platform care-levels · coordinate geometry · origin-transport + turnaround default-flips → GM-4 · **demotion unreachable — CP4 gate #7 (promote-only) is in tension with the ratified D-B "either direction" ruling; the recompute mechanism supports withdrawal, no path reaches it. Trigger: CP4 re-gate or the first authorised demotion mechanism**) · ledger · S3 OUTCOME block. Commit.

**β gate — DoD:** □ T-5-7 green + T-5-7w red-then-green documented □ exploit pinned □ F3 case pinned □ G7 denominator shift reported □ O1 byte-identical end-to-end □ both-OFF byte-identity asserted □ re-measure ratios per stage + drafted provisional ruling □ S3-AMEND-1/2/3 verified in-file □ conservation ×3 (≥1 two-POI) □ suite green; count = baseline + N, N enumerated; deselected M enumerated □ LOC actual vs β estimate □ deviations table □ HALT — Step-4 and GM-4 remain unauthorised. **S3 closes the last heavy intrinsic step.**

## §AMEND — delegated MVP_ACCEPTANCE.md edits (β slice 5 ONLY; verbatim; labelled blocks begin here — S1's two amendments were applied inline, minuted at R4)
**S3-AMEND-1 (AC-1.2, replaces its text):** "AC-1.2 Configure POI-NORTH with a shorter travel-time edge to R1-ALPHA and POI-SOUTH with a shorter travel-time edge to R1-BRAVO (edges encode nearness; coordinate geometry deferred — register). Assert casualties from each POI predominantly route via their shorter-edge R1 under graph routing. [AMENDED at S3: original asserted an unbuilt coordinate mechanism — S3_FOLLOWUP Q18.3.]"

**S3-AMEND-2 (AC-1.4, appends):** "AMENDED at S3: determinism alone is insufficient — with N≥2 POIs, system-axis stream scoping (arrivals:<poi>, mascal_*:<poi>) is MANDATORY for CRN pairing (Q11.4, measured). AC-1.4 asserts BOTH (a) two-POI double-run digest identity at seed=42 and (b) I-2-style paired invariance on a fixed two-POI scenario. Single-POI scenarios retain legacy key forms (byte-compatibility, Q11.4 verified). Cross-POI-count arm comparisons are guarded invalid."

**S3-AMEND-3 (AC-1.1, appends):** "CLARIFIED at S3: weights are shares of the theatre arrival rate via arrivals.per_poi (Σ=1.0±ε guarded); per-POI MASCAL generators inherit the split; the AC fixture runs mascal_enabled=False (detector configurability = #30)."

## Kickoff R (verbatim — may run immediately, docs-only)
NOT plan mode. Execute R1–R5 exactly; nothing outside docs/. Report: both commit SHAs · Rev 4 confirmed · note's signed block quoted · checker + 163 green · porcelain clean. HALT.

## Kickoff α (verbatim — fires only on gate word 1)
NOT plan mode. Process rules 1–2 in force. Step 0: verify R closed — docs/MVP/BUILD_S3.md at HEAD content-identical to this file · ROUTING_SEMANTICS_NOTE.md committed with signed block · CURRENT.md Rev 4 · **163 passed, 0 deselected** · checker ALL PASS · record baseline SHA. Any miss = STOP. Then slice 0 commit → slice 1 red-then-green commit → slice 2 red-then-green commit (incl. gate-time slow-test run) → α DoD → STOP for word 2. Rules: seed=42 · British English · zero-regen (any O1 diff = STOP) · inline-dict fixtures · engine.py:84-121 untouchable · MVP_ACCEPTANCE.md untouched in α · LOC drift materially beyond estimate = STOP · Write tool + ls · nothing outside the fence.

## Kickoff β (verbatim — fires only on gate word 2)
NOT plan mode. Verify α ratified in ledger, HEAD = α close, suite green → slice 3 commit (T-5-7 defaults-pin + T-5-7w; exploit + F3 case pinned) → slice 4 commit + two-stage re-measure (pre-declared only; committed before viewing) → slice 5 §AMEND + Rev 5 commit → β DoD → HALT. Same standing rules; AC edits ONLY via §AMEND.
